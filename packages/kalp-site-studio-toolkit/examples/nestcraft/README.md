# Nestcraft pilot integration example

Nestcraft consumes the core Site Studio field-draft adapter from
`lib/editorUtils.ts`. Its existing same-origin catch-all API route forwards the
authenticated session and configured tenant database scope to Kalp Business
Core. This example is verification evidence only; no Nestcraft or furniture
terminology is present in the core package API, configuration, or defaults.
